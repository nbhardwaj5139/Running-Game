{
  "name": "vitreous",
  "version": "0.1.0",
  "private": true,
  "description": "VITREOUS — a 3D endless runner inside the eye of something asleep. Design docs, Three.js prototype, Shared Nerve server.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "node server/server.js",
    "serve": "python3 -m http.server 8080 --directory prototype"
  },
  "workspaces": [
    "server"
  ],
  "license": "MIT",
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
