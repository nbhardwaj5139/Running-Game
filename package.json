{
  "name": "kitsune",
  "version": "0.2.0",
  "private": true,
  "description": "KITSUNE — 狐: a 3D endless run across Japan. Design docs, zero-build Three.js prototype, synthesised score, room server for racing friends.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "node server/server.js",
    "serve": "python3 -m http.server 8080 --directory prototype"
  },
  "workspaces": [
    "server"
  ],
  "license": "MIT",
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
